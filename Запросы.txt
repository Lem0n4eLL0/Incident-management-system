/api/users/: GET, POST, PUT, DELETE, PATCH

/api/incidents/items/: GET, POST, PUT, DELETE, PATCH
/api/incidents/analytics/report-xlsx/: GET

/api/auth/token/: POST (получение токенов)
/api/auth/token/refresh/: POST (обновление по refresh)

Маршруты, которые есть, но хз пригодятся ли они тебе:
/api/auth/token/verify/: POST (проверка действительности токена)
/admin/: (вход в дефолтную среду админа Django)

/logout_user/*id пользователя*/: POST
/logout_all_users/: POST


/api/incidents/soft_delete_incident/*id инцидента*/: DELETE
/soft_delete_user/*id юзера*/: DELETE

/me/: GET

Создание createsuperuser в shell


from users.models import User, Unit
from users.managers import UserManager 

unit = Unit.objects.get_or_create(name="IT Company")[0]

user = User.all_objects.create_superuser(
    email='Papa@mail.ru', 
    password='12345', 
    login='Papa', 
    full_name='Papa', 
    unit=unit,
    position='администратор', 
    telephone='+1234567890'
)

print("Суперпользователь создан:", user)